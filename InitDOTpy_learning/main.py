from functions.arithmetic import add
print(add(4,5))

from functions import arithmetic
print(arithmetic.add(4,5))

from functions import sub
print(sub(9,5))

#want to initialize something
import logging.config
# logging.config.dictConfig(logging_config)